\begin{enumerate}[leftmargin=1.6em,itemsep=2pt]
\item $F = F_{min}$ exactly (the mismatch term vanishes).
\item A resistor would add its own thermal noise; the inductor sets a real input impedance without adding noise.
\item The first stage (the LNA), by the Friis cascade formula.
\end{enumerate}
