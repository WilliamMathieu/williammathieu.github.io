\begin{enumerate}[leftmargin=1.6em,itemsep=2pt]
\item Its gain $G_1$ divides every later stage's excess noise $(F_i-1)$, so early gain suppresses later noise.
\item $F_1 = 1.26$, $G_1 = 100$, $F_2 = 10$: $F_{tot} = 1.26 + 9/100 = 1.35$, so $\text{NF} = 1.3\ \text{dB}$.
\item The last (high-level) stage.
\end{enumerate}
