\begin{enumerate}[leftmargin=1.6em,itemsep=2pt]
\item Spin echo (its $180^{\circ}$ pulse refocuses static inhomogeneity).
\item $T_1$ weighting.
\item Phase dispersion from static $B_0$ inhomogeneity, so the echo decays with true $T_2$ rather than $T_2^{*}$.
\end{enumerate}
