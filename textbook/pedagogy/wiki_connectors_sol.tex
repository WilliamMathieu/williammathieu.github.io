\begin{enumerate}[leftmargin=1.6em,itemsep=2pt]
\item 2.92~mm --- SMA is only specified to about $18\ \text{GHz}$.
\item Yes; the 2.92~mm (``K'') interface is mechanically compatible with SMA, though careful use preserves precision.
\item It deforms or wears the mating surfaces, degrading the impedance interface and raising reflections (worse return loss).
\end{enumerate}
