\begin{enumerate}[leftmargin=1.6em,itemsep=2pt]
\item $-30 + 12 = -18\ \text{dBm}$.
\item Falls --- the amplifier compresses.
\item The LNA, so its low noise figure dominates the cascade (Friis) before later stages add noise.
\end{enumerate}
