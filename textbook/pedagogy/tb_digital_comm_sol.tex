\begin{enumerate}[leftmargin=1.6em,itemsep=2pt]
\item $C = 5\times10^{6}\log_2(32) = 5\times10^{6}\times5 = 25\ \text{Mbit/s}$.
\item On $E_b/N_0$ (which normalizes out rate and bandwidth).
\item Sharp (short) pulses require large bandwidth; pulse shaping limits bandwidth while controlling inter-symbol interference.
\end{enumerate}
