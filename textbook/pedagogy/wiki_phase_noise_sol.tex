\begin{enumerate}[leftmargin=1.6em,itemsep=2pt]
\item Falls (it is highest close to the carrier).
\item $20\ \text{dB/decade}$ (the $1/f^2$ slope).
\item Less --- $\sigma_t = \sigma_\phi/(2\pi f_c)$, so higher $f_c$ reduces jitter for the same phase error.
\end{enumerate}
