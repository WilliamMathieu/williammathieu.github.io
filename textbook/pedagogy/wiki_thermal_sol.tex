\begin{enumerate}[leftmargin=1.6em,itemsep=2pt]
\item $T_j = 30 + 8\cdot6 = 78\ ^\circ\text{C}$.
\item $\theta_{sa}$, the sink-to-ambient resistance.
\item To gain reliability margin --- lifetime falls roughly exponentially with $T_j$, so headroom greatly extends the mean time to failure.
\end{enumerate}
